# DSCPP5 Quicksort Toolkit

This project mirrors the quicksort lab arc:

- building and managing a raw contiguous buffer
- median-of-three pivot selection
- partitioning around a pivot
- recursive sorting and careful boundary reasoning

The starter keeps the buffer management in place but uses a placeholder sort
so students can replace it with a real quicksort.

